<Window x:Class="DsapiMonitor.MiniWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MiniWindow"
        Width="140" Height="36"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border CornerRadius="14"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Padding="14,0"
            MouseLeftButtonDown="WindowDrag"
            MouseLeftButtonUp="WindowRestore">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#E60E1223"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsDailyLimitExceeded}" Value="True">
                        <Setter Property="Background" Value="#E6EF4444"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" Opacity="0.5" ShadowDepth="1" Color="#000000"/>
        </Border.Effect>
        <TextBlock Text="{Binding BalanceText}"
                   Foreground="{StaticResource AccentGreenBrush}"
                   FontSize="13"
                   FontWeight="SemiBold"
                   FontFamily="Cascadia Code, Consolas"
                   VerticalAlignment="Center"/>
    </Border>
</Window>
